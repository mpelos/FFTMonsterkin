# LIVE PROOF — v1.0 gate PASSED

In-game test (user report): Dragon's Might (ability 253, re-pointed 0x5C -> 0x3B) applied
its stat-up effect to a Chocobo — a target the vanilla Dragon Check force-misses. Also
confirmed working on another monster and on a HUMAN target (expected: the re-point removes
the species gate entirely).

Reloaded log evidence (21:54 launch): "fftivc.beasttamer - Processing 4 cell changes for
nex table 'overrideabilityactiondata'" + testkit "Applying JobCommandData with 4 change(s)"
(clean after the ExtendAbilityIdFlagBits fix).

CONCLUSIONS
- Formula re-point via OverrideAbilityActionData is honored by the engine (first live
  proof of a Formula-byte override in our project history; the neuter had only proven X/Y).
- v1.0 semantics = works on ANY unit. User direction: final goal is MONSTERS-ONLY ->
  v1.1 needs the species gate back, which data cannot express -> code-mod layer.

v1.1 DESIGN (chosen): revert to vanilla formulas 0x5A-0x5D and make the Dragon Check
PASS for monsters by input-control — write the target's live graphic byte to 15 (Dragon)
for the duration of the action when (a) the pending action is ability 251-254 and (b) the
target's job id (+0x03) is in the monster range (94-141 generic; specials TBD). Humans
stay blocked by vanilla's own check; monsters get exact vanilla behavior (Gift keeps its
status-cleanse, Charm/Speed keep flat 100%).
Blockers to resolve first (offline-first): the graphic byte offset in the 0x200 unit
struct, and per-action (ability id, target) resolution timing vs the VM check read.
